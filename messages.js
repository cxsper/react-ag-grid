/*
 * DataGrid Messages
 *
 * This contains all the text for the DataGrid container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DataGrid';

export default defineMessages({
  create: {
    id: `${scope}.create`,
    defaultMessage: 'Create New',
  },
});
